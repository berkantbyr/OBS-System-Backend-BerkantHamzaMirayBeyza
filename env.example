# ==========================================
# Backend Environment Variables
# ==========================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun

# Server Configuration
NODE_ENV=development
PORT=5000
HOST=0.0.0.0

# Database Configuration (Local MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=campus_db
DB_USER=admin
DB_PASSWORD=password

# Database Configuration (Cloud SQL - Production)
# Cloud SQL için socket path kullanın:
# DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# Örnek: DB_HOST=/cloudsql/my-project:europe-west1:obs-db

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-token-secret-change-this
JWT_REFRESH_EXPIRES_IN=30d

# Email Configuration
# Provider: 'sendgrid' (recommended) or 'smtp'
EMAIL_PROVIDER=sendgrid

# SendGrid Configuration (Recommended for Production)
SENDGRID_API_KEY=your-sendgrid-api-key-here
SENDGRID_FROM=system.obs1111@gmail.com

# SMTP Configuration (Fallback or Development)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=system.obs1111@gmail.com
EMAIL_PASS=dgqjgaqyhekshbbz
EMAIL_FROM=system.obs1111@gmail.com
EMAIL_FROM=system.obs1111@gmail.com

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000

# Production Frontend URL
# FRONTEND_URL=https://obs-frontend-214391529742.europe-west1.run.app

